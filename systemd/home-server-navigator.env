# Home Server Navigator env

# Listen address
HOST=0.0.0.0
PORT=8080

# Used to compose clickable URLs in UI (your LAN hostname)
DEFAULT_HOST=localhost

# Persisted data file
DATA_FILE=/var/lib/home-server-navigator/services.json
